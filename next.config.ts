import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The OrbitalHeroSection renders into a canvas entirely on the client, so no
  // special server configuration is required.
};

export default nextConfig;
